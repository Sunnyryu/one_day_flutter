import 'package:flutter/material.dart';

class ThirdAnimatePage extends StatelessWidget {
  const ThirdAnimatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("animatedContinar"),
        centerTitle: true,
        elevation: 0,
        backgroundColor: Colors.yellow,
      ),
      body: Center(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
